# 使用者：姜海波
# 创建时间：2023/4/7  10:33
class Solution:
    def capitalizeTitle(self, title: str) -> str:
        ans = []
        for i in title.split(" "):
            if len(i) > 2:
                title.lower()
            else:
                title.lower()
        return " ".join(ans)
